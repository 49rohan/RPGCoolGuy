@model Fight;
<form asp-action="Fight" method="post">
    <select asp-for="Character1">
        @foreach(Character c in ViewBag.Characters)
        {
            <option value="@c.Id">@c.Name</option>
        }
    </select>
    <select asp-for="Character2">
        @foreach (Character c in ViewBag.Characters)
        {
            <option value="@c.Id">@c.Name</option>
        }
    </select>
    <button type="submit">Submit</button>
</form>